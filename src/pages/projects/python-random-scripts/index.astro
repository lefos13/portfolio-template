---
import content from "../../../content.json";
import ProjectLayout from "../../../components/ProjectLayout.astro";
import BubbleGrid from "../../../components/BubbleGrid.astro";
import Bubble from "../../../components/Bubble.astro";
import MetricGrid from "../../../components/MetricGrid.astro";
import Metric from "../../../components/Metric.astro";

const project = content.pages.projects["python-random-scripts"];
const pageTitle = project.title;
const pageDescription = project.description;
const repoUrl = "https://github.com/lefos13/python-random-scripts";
---

<ProjectLayout
  title={pageTitle}
  description={pageDescription}
  repoUrl={repoUrl}
  theme="purple"
>
  <BubbleGrid ariaLabel="Script categories" theme="purple">
    {
      project.highlights.map((highlight) => (
        <Bubble title={highlight.title}>{highlight.content}</Bubble>
      ))
    }
  </BubbleGrid>

  <h2>What Lives in the Repo</h2>
  <p>
    {project.whatLivesInTheRepo}
  </p>

  <MetricGrid theme="purple">
    {
      project.metrics.map((metric) => (
        <Metric value={metric.value} label={metric.label} />
      ))
    }
  </MetricGrid>

  <h3 id="highlights">Highlights</h3>
  <ul>
    {project.features.map((feature) => <li>{feature}</li>)}
  </ul>

  <h3>Why It Matters</h3>
  <p>
    {project.whyItMatters}
  </p>

  <p class="cta">
    {project.cta}
    <a href={repoUrl} target="_blank" rel="noopener">{repoUrl}</a>.
  </p>
</ProjectLayout>
