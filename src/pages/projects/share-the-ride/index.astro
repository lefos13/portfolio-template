---
import content from "../../../content.json";
import ProjectLayout from "../../../components/ProjectLayout.astro";
import BubbleGrid from "../../../components/BubbleGrid.astro";
import Bubble from "../../../components/Bubble.astro";
import MetricGrid from "../../../components/MetricGrid.astro";
import Metric from "../../../components/Metric.astro";
import StackGrid from "../../../components/StackGrid.astro";

const project = content.pages.projects["share-the-ride"];
const pageTitle = project.title;
const pageDescription = project.description;
const repoUrl = "https://github.com/lefos13/share-the-ride-be";
---

<ProjectLayout
  title={pageTitle}
  description={pageDescription}
  repoUrl={repoUrl}
  theme="blue"
>
  <BubbleGrid ariaLabel="Project highlights" theme="blue">
    {
      project.highlights.map((highlight) => (
        <Bubble title={highlight.title}>{highlight.content}</Bubble>
      ))
    }
  </BubbleGrid>

  <h2>What I Built</h2>
  <p>
    {project.whatIBuilt}
  </p>

  <MetricGrid theme="blue">
    {
      project.metrics.map((metric) => (
        <Metric value={metric.value} label={metric.label} />
      ))
    }
  </MetricGrid>

  <h3>Highlights</h3>
  <ul>
    {project.features.map((feature) => <li>{feature}</li>)}
  </ul>

  <h3 id="architecture">Architecture Glimpse</h3>
  <p>
    {project.architecture}
  </p>

  <h3>Stack at a Glance</h3>
  <StackGrid theme="blue">
    <div>
      <h4>Core</h4>
      <p>{project.stack.core}</p>
    </div>
    <div>
      <h4>Messaging & Auth</h4>
      <p>{project.stack.messagingAuth}</p>
    </div>
    <div>
      <h4>Operational</h4>
      <p>{project.stack.operational}</p>
    </div>
  </StackGrid>

  <h3>Outcome</h3>
  <p>
    {project.outcome}
  </p>

  <p class="cta">
    {project.cta}
  </p>
</ProjectLayout>
